# CV Assignment 2 — Problem Statement 3: Advanced Object Tracking and Detection in Video Streams

Course: Computer Vision (S2-25_AIMLCZG525) — Assignment 2 (15 pts total)
Source spec: `Assignment 2  .pdf` (this directory) — read it directly if any instruction below seems ambiguous, it is the source of truth.

## Task summary

Build a multi-object detection + tracking system:
- **Detection**: Faster R-CNN, fine-tuned on the selected dataset.
- **Tracking**: temporal consistency checks so detected objects keep a stable identity across frames, plus an adaptive tracking algorithm (Kalman filter / SORT) that adjusts to object speed and direction.
- **Dataset**: [SportsMOT](https://deeperaction.github.io/datasets/sportsmot.html) (fast-moving multi-athlete sports video — good stress test for identity switches).
- Reference papers (for the notebook's related-work/justification section, not for copying): arXiv:2006.04567 ("Real-Time Object Detection and Tracking Using Faster R-CNN"), ScienceDirect S0031320321001234 ("A Survey on Object Detection and Tracking").

## Grading rubric → notebook mapping

Structure the notebook so each rubric criterion is a clearly labeled section — this is how it gets graded, so 1:1 traceability matters more than clever notebook organization.

| Rubric criterion | Pts | Notebook section |
|---|---|---|
| Data Preprocessing | 2.5 | `1. Data Preprocessing` — frame extraction, normalization/resizing, augmentation (random crop, flip, color jitter) |
| Model Development | 5 | `2. Model Development` — Faster R-CNN fine-tuning, temporal consistency check, adaptive tracking (Kalman/SORT) |
| Evaluation Metrics | 2.5 | `3. Evaluation` — mAP, tracking accuracy (MOTA/MOTP), identity switch rate, inference speed |
| Justification | 2.5 | `4. Analysis & Justification` — explain results, over/underfitting, why chosen design choices helped or hurt |
| Documentation / Presentation / Code Quality | 2.5 | Markdown intro + conclusion, clean code throughout, link to the 4-min video |

## Environment — Google Colab (no local env, no uv)

Everything runs inside the Colab notebook itself. There is no local `pyproject.toml`/`uv.lock` to maintain — all setup happens in the notebook's first cell(s) so the notebook is self-contained and reproducible by anyone who opens it fresh in Colab.

**First cell of the notebook:**
```python
# Runtime > Change runtime type > T4 GPU (or better) before running this
!pip install -q py-motmetrics filterpy
# torch, torchvision, opencv-python, numpy, pandas, matplotlib, tqdm, scikit-learn
# already ship in the Colab base image — no need to reinstall them
```

- Confirm GPU is active early: `import torch; print(torch.cuda.is_available(), torch.cuda.get_device_name(0))` — record this in the notebook since inference speed (FPS) is an evaluation metric and depends on which GPU Colab assigned you.
- Pin package versions only if you hit a compatibility break (e.g. a specific `torchvision`/`torch` pairing) — otherwise let Colab's preinstalled versions stand, they're already mutually compatible.
- Colab sessions are ephemeral: the VM, its disk, and any `pip install`s reset when the runtime recycles or after ~12h/idle timeout. Anything you want to keep (checkpoints, extracted frames, the notebook itself) must go to Google Drive or be re-generated by re-running cells from the top.

**Persisting data/checkpoints across sessions:**
```python
from google.colab import drive
drive.mount('/content/drive')
# e.g. work under /content/drive/MyDrive/cv_assignment2/
```
Download the SportsMOT subset straight into the ephemeral `/content/` disk each session (fast Colab network, avoids burning Drive quota) — only persist trained checkpoints and final outputs to Drive.

## Project layout

Local machine (this repo — for planning/editing with Claude Code, not executed here):
```
assignment_2/
├── CLAUDE.md
├── Assignment 2  .pdf          # original spec, do not modify
└── notebooks/
    └── CV_assignment2_<group>_PS3.ipynb   # authored locally, then opened/run in Colab
```

Since Colab runs a single self-contained notebook (no local `src/` package to import from unless you push it somewhere Colab can reach), keep the notebook itself well-organized instead of splitting into modules:
- Group reusable code (dataset class, Kalman/SORT tracker, metrics helpers) into a few dedicated code cells near the top, under clear markdown headers (e.g. "Utility: Dataset", "Utility: Tracker", "Utility: Metrics") — this gets you most of the readability benefit of separate files without needing multi-file imports in Colab.
- If the tracker/metrics logic gets long, an alternative is to push `src/*.py` to a GitHub repo and `!git clone` it as the first Colab cell — only worth it if you're already using git for this project.

The rubric explicitly penalizes "excessively long notebooks" and "irrelevant lengthy prints" — the notebook should read as a narrated report (markdown explanation → short code cell → key output/plot), not a dump of training logs. Use `tqdm` for progress bars instead of per-epoch prints, and only display summary metrics/plots, not raw tensors or full prediction dumps.

## Notebook section outline

1. **Intro** (markdown) — problem statement, dataset, approach summary.
2. **Data Preprocessing**
   - Extract frames from SportsMOT clips, normalize (resize + scale/mean-std normalization to match Faster R-CNN's expected input).
   - Augmentation: random crop, horizontal flip, color jitter — applied only to training split.
3. **Model Development**
   - Load `torchvision`'s Faster R-CNN (ResNet-50-FPN backbone), replace the classification head for the dataset's classes, fine-tune.
   - Temporal consistency: check detections across consecutive frames (e.g. IoU-based matching) before accepting an identity.
   - Adaptive tracking: implement SORT (Kalman filter for motion prediction + Hungarian matching), tuned to adapt to object speed/direction.
4. **Evaluation**
   - Detection: mAP (via `torchmetrics` or manual COCO-style eval).
   - Tracking: MOTA, MOTP, ID-switch count, IDF1 — use `py-motmetrics` rather than hand-rolling these.
   - Report inference speed (FPS) since it's called out explicitly.
   - Optional: compare against a baseline tracker (e.g. plain IoU tracker without Kalman) to show the adaptive tracker's benefit.
5. **Analysis & Justification** (markdown-heavy) — where tracking failed (occlusion, fast motion, crowded scenes), why, and what the temporal-consistency/adaptive-tracking additions did or didn't fix.
6. **Conclusion** — summary of findings + placeholder for the video presentation link.

## Submission constraints (from the PDF, don't lose these)

- File name: `CV_assignment2_<group>_<problemstatementnumber>` (this is PS3 → e.g. `CV_assignment2_group1_3`).
- Submit as Jupyter notebook **converted to HTML or PDF, with outputs already run and visible**. From Colab:
  - Run all cells top to bottom first (`Runtime > Run all`) so every output is actually rendered and saved into the `.ipynb`.
  - `File > Download > Download .ipynb` for the raw notebook, and/or `File > Print > Save as PDF` for a PDF snapshot of the executed notebook.
  - For a cleaner HTML export with all outputs: `!jupyter nbconvert --to html "CV_assignment2_<group>_PS3.ipynb"` run as a Colab cell, then download the resulting `.html` from the file browser.
- Data files are not uploaded — only the notebook/HTML/PDF.
- 4-minute video walkthrough of the approach + a live code run is required; the link goes in the group's row of the shared Google Sheet (this is a manual submission step, not something to automate here).
- Comparing against baseline/SOTA trackers is explicitly optional — don't over-invest there at the expense of the mandatory sections.
